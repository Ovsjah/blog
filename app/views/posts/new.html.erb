<h1>New Post</h1>

<%= render 'form', post: @post %>

<% if @current_category %>
  <%= link_to "Back to category with posts", category_path(@current_category) %>
<% else %>
  <%= link_to "Back to categories", categories_path %>
<% end %>
